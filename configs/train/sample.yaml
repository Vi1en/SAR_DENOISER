# SAMPLE processed patches — same pipeline as default; alternate checkpoint dir for experiments.
# Ignored keys like ``use_sample_dataset`` are for documentation only.

seed: 42
device: auto
data_dir: data/sample_sar/processed
patch_size: 128
batch_size: 8
num_workers: 2
epochs: 20
lr: 0.0002
model_type: unet
noise_conditioning: false
checkpoint_dir: checkpoints_sample
results_png: improved_training_results.png
use_sample_dataset: true
