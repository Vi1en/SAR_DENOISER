# Streamlit Community Cloud: in Manage app → Advanced settings, prefer Python 3.11 for
# parity with CI and smaller CPU stacks. Python 3.13 is supported via the ranges below.
#
# No --extra-index-url here: `uv pip install` on Community Cloud breaks when the PyTorch
# index is listed first (setuptools / transitive build deps). Torch wheels are on PyPI.
setuptools>=70.0.0
wheel
streamlit>=1.28.0
numpy>=1.26.4,<2.2.0
scipy>=1.11.0,<1.16.0
matplotlib>=3.7.0,<3.11
Pillow>=9.0.0,<13
opencv-python-headless>=4.8.0,<4.13
torch>=2.2.2,<2.8.0
torchvision>=0.17.2,<0.27.0
PyYAML>=6.0
scikit-image>=0.21.0,<0.26.0
tqdm>=4.64.0
packaging>=20.0,<26
