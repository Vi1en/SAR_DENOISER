# PyTorch: install from PyPI only here so Streamlit Cloud’s uv resolver does not mix indexes
# (extra-index-url + uv breaks builds of onnx/ml-dtypes/setuptools). CI/Docker install CPU
# wheels explicitly where needed; see .github/workflows/ci.yml and Dockerfile.
PyYAML>=6.0
torch>=1.12.0
torchvision>=0.13.0
numpy>=1.21.0,<2.0
scipy>=1.8.0,<1.14
scikit-image>=0.19.0
opencv-python-headless>=4.6.0,<4.13
matplotlib>=3.5.0,<3.10
streamlit>=1.20.0
tqdm>=4.64.0
Pillow>=9.0.0,<12
packaging>=20.0,<25
pandas>=1.4.0
seaborn>=0.11.0
plotly>=5.0.0
